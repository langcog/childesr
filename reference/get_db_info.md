# Get information on database connection options

Get information on database connection options

## Usage

``` r
get_db_info()
```

## Value

List of database info: host name, current version, supported versions,
historical versions, username, password

## Examples

``` r
if (FALSE) { # \dontrun{
get_db_info()
} # }
```
